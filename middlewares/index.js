const { validateBody } = require("./validateBody");
const { validateBodyFavorite } = require("./validateBody");
const isValidId = require("./isValidId");

module.exports = {
  validateBody,
  validateBodyFavorite,
  isValidId,
};
